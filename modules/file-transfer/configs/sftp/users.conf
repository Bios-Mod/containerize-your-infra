# Deploy to:   modules/file-transfer/configs/sftp/users.conf
# Apply:       mounted at /etc/sftp/users.conf:ro — docker compose up
# Module:      file-transfer
# Requires:    configs/keys/labuser_ed25519.pub mounted at /home/labuser/.ssh/keys/
#
# SFTP user definitions for atmoz/sftp.
# Parameters modified from baseline: password field empty (key-only auth), UID 1001, upload subdir
#
# Syntax: user:pass[:e][:uid[:gid[:dir1[,dir2]...]]]
# Empty password disables password authentication — SSH key required.

labuser::1001::upload