# Deploy to:   configs/bind/named.conf
# Apply:       docker compose up -d (bind mount → /etc/bind/named.conf)
# Module:      dns
# Requires:    named.conf.options, named.conf.local
#
# Main BIND9 entry point — includes options and local zone definitions.
# Parameters modified from baseline: none

include "/etc/bind/named.conf.options";
include "/etc/bind/named.conf.local";