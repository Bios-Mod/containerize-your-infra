# Deploy to:   GitHub Actions runner
# Apply:       automatic on push (paths filter)
# Module:      dns
# Requires:    none
#
# Validates the DNS module Compose file — syntax check and image resolution.
# No custom build: BIND9 uses the official image.
# Parameters modified from baseline: none

name: dns

on:
  push:
    branches: main
    paths:
      - "modules/dns/docker/**"
      - "modules/dns/README.md"
      - ".github/workflows/dns.yml"

jobs:
  validate:
    runs-on: ubuntu-24.04-arm
    defaults:
      run:
        working-directory: modules/dns/docker/

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Validate development Compose syntax
        run: docker compose -f docker-compose.yml config

      - name: Validate production Compose syntax
        run: docker compose -f docker-compose.prod.yml config

      - name: Pull development image
        run: docker compose -f docker-compose.yml pull

      - name: Pull production image
        run: docker compose -f docker-compose.prod.yml pull